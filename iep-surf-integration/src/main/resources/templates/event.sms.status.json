{
  "configId": "status_sms",
  "attributes": {
    "msisdn": "${msisdn}",
    "status": "${status}",
    "text": "${text}",
    "id_correlacao": "${correlationId}",
    "data_requisicao": "${date}"
  }
}