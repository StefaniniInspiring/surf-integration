{
  "configId": "resposta_sms",
  "attributes": {
    "texto_sms": "${text}",
    "origem": "${shortNumber}",
    "id_correlacao": "${correlationId}",
    "data_requisicao": "${date}"
  },
  "actors": [{
    "alias": "cliente",
    "keyName": "msisdn",
    "keyValue": "${msisdn}",
    "attributes": {
      "msisdn": "${msisdn}"
    }
  }]
}