{
  "configId": "status_sms",
  "attributes": {
    "status": "${status}",
    "text": "${text}",
    "id_correlacao": "${correlationId}",
    "data_requisicao": "${date}"
  },
  "actors": [{
    "alias": "cliente",
    "keyName": "msisdn",
    "keyValue": "${msisdn}",
    "attributes": {
      "msisdn": "${msisdn}"
    }
  }]
}